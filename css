/* styles.css */

/* Basic styles for header, navigation, sections, and footer */
header, footer {
    background-color: #333;
    color: #fff;
    text-align: center;
    padding: 10px 0;
}

nav ul {
    list-style-type: none;
    padding: 0;
    text-align: center;
}

nav ul li {
    display: inline;
    margin: 0 10px;
}

nav ul li a {
    color: #fff;
    text-decoration: none;
}

main {
    padding: 20px;
}

section {
    margin-bottom: 20px;
}

/* Styles for the calendar display */
#calendar-container {
    display: grid;
    grid-template-columns: repeat(4, 1fr); /* Example grid layout */
    gap: 10px;
}

/* Additional styles can be added for form elements, etc. */
